import { Alert, Box, Button, Chip, Stack, Typography } from '@mui/material';
import { Settings } from '@mui/icons-material';
import SectionPaper from '../components/common/SectionPaper';

export default function PaymentsPage({ providers, settings }) { return <Stack spacing={3}><Box><Typography variant="h5">Платежи</Typography><Typography color="text.secondary">Статус подключённых платёжных систем</Typography></Box><SectionPaper sx={{ p: 2.5 }}><Typography variant="h6">Платежные системы</Typography><Typography variant="body2" color="text.secondary">Заготовки для будущей интеграции оплат и транзакций.</Typography></SectionPaper><Box sx={{ display: 'grid', gap: 2, gridTemplateColumns: { xs: '1fr', md: 'repeat(3, 1fr)' } }}>{providers.map((provider) => <SectionPaper key={provider.id} sx={{ p: 2.25 }}><Stack spacing={2}><Stack direction="row" justifyContent="space-between" alignItems="center"><Typography fontWeight={800}>{provider.name}</Typography><Chip size="small" label={provider.status} /></Stack><Button startIcon={<Settings />} variant="outlined" disabled>Настройки</Button></Stack></SectionPaper>)}</Box><Alert severity="info">Статусы платежей: {(settings.statuses || []).join(', ')}</Alert></Stack>; }
